---
import MainLayout from "../layouts/MainLayout.astro";
import "../styles/global.css";
import Button from "../components/Button.astro";
---

<MainLayout>
  <section class="flex flex-col items-center gap-space25">
    <article id="single_img">
      <div class="single_img_wrapper max-w-[300px]">
        <img src="../img/singleview_mobil.png" alt="produktbillede af cider" />
      </div>
    </article>
    <article id="single_tekst">
      <div class="single_tekst_wrapper flex flex-col gap-space25">
        <h1 class="font-text font-bold text-mobileH1 text-black600">
          Cidrerie de l’Apothicaire
        </h1>
        <h2 class="pris font-text font-semibold text-mobileH2 text-grey500">
          169 kr.
        </h2>
        <p class="font-text flex flex-col text-black500 text-mobileP">
          Cideren er vildgæret og økologisk, og har en fed gæret sprød smag, med
          en let naturlig aroma. Hors-Norme er La Ciderie de Golfes klassiske
          cider. La Ciderie de Golfe er en lille naturciderproducent ledet af
          Marc Abel, som producerer alt på tradionelt low-tech metode, hvor alt
          er vildgæret og økologisk.
        </p>
        <div class="info">
          <p class="font-text text-mobileP text-black500">
            <span class="font-bold">Område:</span> Bretagne, Frankrig
          </p>
          <p class="font-text text-mobileP text-black500">
            <span class="font-bold">Vol:</span> 5,5 %
          </p>
        </div>
        <div class="flex flex-col w-full items-center">
          <div
            id="container"
            class="flex border-green100 border-2 w-[75%] justify-between"
          >
            <button
              class="font-bold px-space10 border-green100 border-r-2 font-text text-mobileH3 text-black500"
              id="minus">-</button
            >
            <span
              class="font-bold font-text px-space10 text-mobileH3 text-black500"
              id="number">0</span
            >
            <button
              class="font-bold px-space10 border-green100 border-l-2 font-text text-mobileH3 text-black500"
              id="plus">+</button
            >
          </div>
        </div>
        <div class="flex w-full justify-center">
          <Button
            href="/kurv"
            variant="primary"
            knapTekst="Tilføj til kurv"
            kurv={true}
          />
        </div>
      </div>
    </article>
  </section>
  <script>
    let number = 0;

    const minusButton = document.querySelector("#minus");
    const plusButton = document.querySelector("#plus");
    const numberElement = document.querySelector("#number");

    minusButton.addEventListener("click", () => {
      if (number === 0) {
        return;
      }
      number--;
      numberElement.textContent = number.toString();
    });

    plusButton.addEventListener("click", () => {
      number++;
      numberElement.textContent = number.toString();
    });
  </script>
</MainLayout>
